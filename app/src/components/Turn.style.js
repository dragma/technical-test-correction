import styled from 'styled-components';
import { Card } from 'react-native-elements';

export const TurnContainer = styled(Card)`
  position: relative;
`;

export const TurnPlayContainer = styled.View`
  position: absolute;
  top: 0px;
  right: 0px;
`;
